services:
  db:
    image: postgres:15-alpine
    container_name: pinball-db-dev
    restart: always
    environment:
      POSTGRES_USER: dev_user
      POSTGRES_PASSWORD: dev_password
      POSTGRES_DB: pinball_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_dev_data:/var/lib/postgresql/data

  server:
    build:
      context: .
      dockerfile: apps/server/Dockerfile
    container_name: pinball-server-dev
    working_dir: /app/apps/server
    environment:
      DATABASE_URL: "postgresql://dev_user:dev_password@db:5432/pinball_dev?schema=public"
      NODE_ENV: development
    ports:
      - "3334:3001"
    volumes:
      - ./apps/server/src:/app/apps/server/src
    depends_on:
      - db
    command: bun --watch src/interface/index.ts

  playfield:
    build:
      context: .
      dockerfile: apps/playfield/Dockerfile
    container_name: pinball-playfield-dev
    working_dir: /app/apps/playfield
    environment:
      NODE_ENV: development
      WATCHPACK_POLLING: "true"
      CHOKIDAR_USEPOLLING: "true"
    ports:
      - "3333:3000"
    volumes:
      - ./apps/playfield/src:/app/apps/playfield/src
      - ./packages/game-engine/src:/app/packages/game-engine/src
    depends_on:
      - server
    command: bun run dev

  scoreboard:
    build:
      context: .
      dockerfile: apps/dmd/Dockerfile
    container_name: pinball-scoreboard-dev
    working_dir: /app/apps/scoreboard
    environment:
      NODE_ENV: development
      WATCHPACK_POLLING: "true"
      CHOKIDAR_USEPOLLING: "true"
    ports:
      - "3335:3000"
    volumes:
      - ./apps/scoreboard/src:/app/apps/scoreboard/src
    depends_on:
      - server
    command: bun run dev

  leaderboard:
    build:
      context: .
      dockerfile: apps/backglass/Dockerfile
    container_name: pinball-leaderboard-dev
    working_dir: /app/apps/leaderboard
    environment:
      NODE_ENV: development
      WATCHPACK_POLLING: "true"
      CHOKIDAR_USEPOLLING: "true"
    ports:
      - "3336:3000"
    volumes:
      - ./apps/leaderboard/src:/app/apps/leaderboard/src
    depends_on:
      - server
    command: bun run dev

volumes:
  postgres_dev_data:
