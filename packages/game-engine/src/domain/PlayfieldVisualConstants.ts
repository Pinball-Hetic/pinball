export const PLAYFIELD_TONE_MAPPING_EXPOSURE = 1.3;
export const PLAYFIELD_MAP_COLOR_DARKEN = 0.9;

/** envMapIntensity pour matériaux très métalliques (or, gemmes metalness ≥ 0.5). */
export const PLAYFIELD_ENV_METALLIC = 1.0;
/** envMapIntensity pour matériaux semi-métalliques (metalness 0.2–0.5). */
export const PLAYFIELD_ENV_SEMI = 1.0;
/** envMapIntensity de base pour matériaux non-métalliques. */
export const PLAYFIELD_ENV_BASE = 1.1;
